
let deferredPrompt=null;
function ensureInstallPromptUI(){let bar=document.getElementById('pwaInstallBar');if(!bar){bar=document.createElement('div');bar.id='pwaInstallBar';bar.className='pwa-install-bar hidden';bar.innerHTML=`<div class="pwa-install-content"><div><strong>Meyvəçi.az tətbiqini quraşdır</strong><p>Saytı tətbiq kimi daha rahat və sürətli istifadə et.</p></div><div class="pwa-install-actions"><button id="pwaInstallBtn" class="btn btn-primary">Quraşdır</button><button id="pwaDismissBtn" class="btn btn-soft">Bağla</button></div></div>`;document.body.appendChild(bar);}return bar;}
function isIos(){return /iphone|ipad|ipod/i.test(window.navigator.userAgent)}
function isInStandaloneMode(){return window.matchMedia('(display-mode: standalone)').matches || window.navigator.standalone===true}
function showIosInstallHint(){if(!isIos()||isInStandaloneMode()) return; if(localStorage.getItem('meyveci_ios_install_dismissed')==='1') return; let hint=document.getElementById('iosInstallHint'); if(hint) return; hint=document.createElement('div'); hint.id='iosInstallHint'; hint.className='pwa-install-bar'; hint.innerHTML=`<div class="pwa-install-content"><div><strong>iPhone / iPad üçün quraşdırma</strong><p>Safari-də paylaş düyməsinə vur və “Add to Home Screen” seç.</p></div><div class="pwa-install-actions"><button id="iosHintDismissBtn" class="btn btn-soft">Bağla</button></div></div>`; document.body.appendChild(hint); document.getElementById('iosHintDismissBtn')?.addEventListener('click',()=>{localStorage.setItem('meyveci_ios_install_dismissed','1');hint.remove();});}
function initPWAInstall(){const bar=ensureInstallPromptUI();window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;if(!isInStandaloneMode()&&localStorage.getItem('meyveci_pwa_dismissed')!=='1'){bar.classList.remove('hidden');}});window.addEventListener('appinstalled',()=>{deferredPrompt=null;bar.classList.add('hidden');});document.addEventListener('click',async e=>{if(e.target?.id==='pwaInstallBtn'){if(!deferredPrompt) return;deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;bar.classList.add('hidden');}if(e.target?.id==='pwaDismissBtn'){bar.classList.add('hidden');localStorage.setItem('meyveci_pwa_dismissed','1');}});showIosInstallHint();}
